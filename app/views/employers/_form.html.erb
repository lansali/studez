<%= form_with(model: employer, class: 'lean_card', local: true) do |form| %>
  <% if employer.errors.any? %>
    <div class="">
      <h2><%= pluralize(employer.errors.count, "error") %> prohibited this employer from being saved:</h2>

      <ul>
        <% employer.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="">
    <%= form.label :user_id %>
    <%= form.text_field :user_id, class: '', placeholder: 'User id' %>
  </div>

  <div class="">
    <%= form.label :bio %>
    <%= form.text_area :bio, class: '', placeholder: 'Bio' %>
  </div>

  <div class="">
    <%= form.label :title %>
    <%= form.text_field :title, class: '', placeholder: 'Title' %>
  </div>
  <br/>

  <div class="actions">
    <%= form.submit 'Submit', class: ""%>
  </div>
<% end %>
